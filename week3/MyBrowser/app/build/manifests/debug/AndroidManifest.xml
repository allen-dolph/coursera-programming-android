<!-- From: file:/Users/allendolph/Github/allen-dolph/coursera-programming-android/week3/MyBrowser/app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1" android:versionName="1.0" package="course.labs.intentslab.mybrowser">

    <uses-sdk android:minSdkVersion="13" android:targetSdkVersion="18"/>

    <application android:allowBackup="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:theme="@style/AppTheme">
        <activity android:label="@string/app_name" android:name="course.labs.intentslab.mybrowser.MyBrowserActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>

                <action android:name="android.intent.action.VIEW"/>
                <data android:scheme="URL"/>
            </intent-filter>
  
            <!-- TODO - Add necessary intent filter information so that this
            			    Activity will accept Intents with the 
            			    action "android.intent.action.VIEW" and with an "http" 
            			    schemed URL -->
            <!-- intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <data android:scheme="URL" />
            </intent-filter-->
        </activity>
    </application>

</manifest>
